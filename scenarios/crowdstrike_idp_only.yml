# CrowdStrike Falcon IDP — standalone replay
# Refreshes product=idp telemetry into crowdstrike_falcon_event_raw,
# including location_country_code, so the IDP correlation rule's geo mapping resolves.
#
#   python3 tools/replay_scenario.py --manifest scenarios/crowdstrike_idp_only.yml
#
compress_window: 30m
sources:
  - name: CrowdStrike IDP
    file: input_tsv/CrowdStrike-IDP-Turla-Carbon-Synthetic.tsv
    env: .env-brumxdr-crowdstrike
